# Ignore npm CLI native build output

## User Request

Clarify whether `packages/open-browser-use-cli/native/` is useful, and either delete it or add it to gitignore if it is generated output.

## Changes

- Confirmed that `packages/open-browser-use-cli/native/` is generated by `scripts/build-npm-cli-package.sh` during npm package preparation.
- Added the generated native binary payload directory to `.gitignore` so local package builds do not leave untracked binaries in the working tree.

## Affected Files

- `.gitignore`
